## 一.RAG 
RAG检索增强生成是一种架构模式：先从外部知识库检索相关内容，再把内容作为上下文交给大模型生成答案。
解决的痛点是：大模型的幻觉问题、知识过时问题、私有数据问题。
核心流程是私有内容上传通过解析切块成小段文本，引用embedding向量化把文本变成一串数字向量。用户问题也会被转成向量，然后和知识库里面的块向量做相似度计算（最常见的相似度计算方法是余弦相似度和点积以及欧式距离，因为文本embedding 更强调语义方向的一致性，余弦相似度更适合。点积没有归一化容易受到向量长度影响。欧式距离是绝对空间距离，在高维语义向量中会受到尺度和距离集中的影响)。计算方式是：点积除以模长乘积
### 1.分块：是为了把大文档拆成小知识块。解决的是文档太长超过了模型上下文长度。
常见的分块有1.固定长度切块，按固定token切分，适合普通文档。2.滑动窗口分块，每块之前保留一部分重叠内容。3.段落分块，根据自然段、标题、换行切。4.按标题层级分块，根据Markdown标题或文档目录切，比如#号。5.语义分块，根据语义相似度切分，把同一主题内容放在一起。6、父子分块，小块用于检索，大块用于回答。 一般企业中用的是混合分块，如递归字符分块+固定长度+滑动窗口+父子分块一起用。

### 2.混合检索：向量语义检索是计算查询向量与知识库向量进行相似度计算，适合单纯的知识问题。但是涉及到带大量专有名词的场景效果就不好。
结合稀疏检索也就是关键词检索来进行多路并行检索。其中关键词稀疏检索是利用BM25算法，会统计查询词在文档中出现的次数，并根据出现次数对文档进行排序；查询方式同时并行方式进行，一次将查询转为向量在知识库中进行检索，另一次则直接进行关键词检索，然后使用融合算法根据得分合并结果，最常用的融合算法是加权求和，它会调整稠密和稀疏检索的比例，一般是70%稠密+30%稀疏；另一种常用的方法是倒数排名融合RRF，按照各个检索器的排名结果进行融合，尤其是在专业术语较多的领域场景，混合检索兼顾了速度、精确度以及召回率，所以它最常用。

### 3.Mivlus 索引：稠密索引构建HNSW近似最近邻图索引，原理是给向量构建多层图结构，查询时从高层快速跳转，逐层下降到低层，找到最相似的向量，在向量入库后创建；而稀疏索引构建倒排索引，关键词到文档的反向映射。查询阶段利用这些索引快速找到最相关的块。
### 向量数据库有哪些？为什么用Mivlus数据库？
Milvus、Qdrant、Faiss、Chroma
四大核心维度：
数据规模：PB级 vs TB级 vs GB级
查询性能：高并发吞吐 vs 低延迟精准
多模态需求：是否涉及图文音视频
部署成本：分布式运维 vs 轻量级嵌入
推荐：
高并发+多模态：Mivlus + Qrdant
快速验证+离线处理：Chroma + Faiss
长期规划：考虑横线扩展能力Mivlus 与云原生支持
未来趋势：云原生向量数据库（如Amazon OpenSearch Serverless）自动扩缩容

### 4.评测指标：
RAG评估的三大维度：1.最终结果准确性 2.大模型的能力 3.检索阶段的效果

### 检索阶段指标：
召回率@Recall：准确检索到的知识与所有相关知识的对比
精确率@Precision：准确检索到的知识与检索到的知识对比
场景：
方案一：追求极致准确率：严格限制检索范围，确保给模型的每一条信息都相关，通过设置较高的相似度阈值，过滤边缘相关的文档。适合对答案准确性要求极高，且知识库质量非常好的场景
方案二：优先优化召回率：尽可能扩大检索范围，降低相似度阈值，让更多候选文档进入大模型视野。因为大模型越来越智能，对噪声数据容忍度高，而且目前企业的知识库文档大部分质量没有高，所以都是优先召回率。
评估方法：1.人工评估：需要多名评估员独立打分，上线前检验用户体验。
模型自动评估：利用 Cross-Encoder/Rerank 计算相关性，快速给出评分。
生成质量阶段：
忠实度Faithfuless ：衡量生成答案与给定上下文之间的事实一致性。答案评分范围在0到1之间，分数越高越好。score：把生成的答案分成n个陈述句，那分块拿去检索，看是不是在上下文里面，如果都在就是1，如果都不在就是0.
答案相关度Answer Relevance：评估生成的答案与给定提示的相关程度。如果答案不完整则被赋予较低的分数。score：把生成的答案生成n个问题，把n个问题与真实的query计算余弦相似度，最后取均值来代表相关性。

### Ragas步骤：
建立评估数据集-问题+真实答案(真实自己造)-大模型答案 [] 空列表-检索上下文 [] 空列表
得出context_precision:文本精确率，context_recall:文本召回率,用于提升embedding模型的准确率测试
faithfuless忠实度和answer_relevancy答案相关性用于大语言模型评测。

## 二、python后端
基于 **Pydantic** 完成接口强校验与 **Schema** 规范化：
Pydantic 是 FastAPI 中常用的数据校验库，主要用于定义请求参数和响应结果的数据模型。通过Pydantic 的 BaseModel 可以声明字段类型、必填规则、默认值和格式校验。Schema 则是接口数据结构规范，用来约束接口入参和出参格式，保证前后端交互联调的数据一致性。

设计**异步任务架构 task_id + 状态机**：
用户提交一个耗时任务后，接口不直接等任务执行完，而是先返回一个 `task_id`。后端在后台慢慢处理任务，并用“状态机”记录任务当前处于哪个阶段，比如：待处理、处理中、成功、失败。前端再拿 `task_id` 去查询任务进度和结果。
任务查询与前台渐进轮询机制提升任务可追踪性

**Redis TTL** 缓存类目结果 + 向量
我的类目推荐流程里，商品标题或类目名需要先转成 embedding 向量，再去数据库做相似类目检索。如果每次请求都重新调用 embedding 模型和向量库，响应会比较慢，所以我加了一层 Redis TTL 缓存。系统会先根据站点、商品标题、图片 URL、类目名等字段构造 cache key，优先查询 Redis。如果命中类目结果，就直接返回；如果没有命中，再看是否缓存过 embedding 向量，能复用就复用，不能复用才重新调用模型生成向量。TTL 可以避免缓存长期不更新导致类目结果过旧。

**SFT** 数据工程与 **LoRA** 多适配器策略：
Lora是一种2021年论文提出的微调策略，不直接更新大模型原始权重，而是在原权重旁边加一个低秩增量矩阵，只训练这个小矩阵，就能低成本的达到理想的结果。
配合SFT监督微调使用，教模型怎么说话。把商品卖点词和商品描述整理成 instruction/input/output 格式，适配 LLaMA-Factory 的 Alpaca 数据格式，并按训练集、验证集、测试集进行切分。
采用大模型网关统一配置不同的模型，由vLLM 加载 Qwen2.5-14B 基座模型和多个 LoRA adapter。
Qwen2.5-14B-Instruct，GPU5090 32GB 单卡 ，内存64GB。
保守50 tokens /s.

项目：
## 项目一：TraceAgentic RAG——多步自主知识智能体
具有自主迭代检索的RAG系统。核心流程是Agent根据意图识别判断是否调用知识库工具，进入LangGraph RAG流程，经过初次混合检索、模型评估、自主选择是否扩展重写，重写有三重策略，分别是回退、假设性文档、和复杂任务，进行二次向量数据库混合检索，召回结果融合返回给LLM结合问题输出。
细节：
### 1.Milvus 向量库实现 Offline 文档父子分块构建混合索引
离线状态：文档清洗解析通过混合模式父子切块处理成子块，并把子块生成稠密向量和稀疏向量以及父块信息一起写入 Mivlus.在此之前，进行构造Mivlus索引的时候，稠密索引构造的是近似最近邻图索引，查询复杂度是O(logn)，稀疏索引构造倒排索引。为子块的稠密和稀疏建索引保存。
### 2.Online 用户query 通过混合检索并 RRF 融合精排rerank召回.
用户问题生成稠密向量和稀疏向量通过混合检索多路召回Topk，进行RRF融合，得到TOP子块根据父块id聚合父块，在进行去重和引用rerank模型进行重排，返回文本给LLM。
### 3.会话长短期 Memory 机制，利用Redis 、MySQL 持久化 、上下文压缩摘要方法
短期记忆存在redis，chat_messages缓存某个用户某个会话的消息列表,chat_sessions缓存某个用户的会话列表；
长期记忆存在数据库，存取session_id 和 user_id以及对话消息。读取流程：先从redis读取，没读到就去数据库查询。
上下文压缩：在上下文的历史消息设置压缩机制，如果历史消息小于50条，直接送给llm，如果历史消息超过50条，就把前40条做摘要，摘要变成一条系统信息，后面的信息原样保留。

其他的记忆方法：存文档

### 4.MCP Gateway 统一管理多协议远程 MCP 服务
MCP 和 function calling 是 tool calling的主要两种调用工具的方法

MCP 是


用 JSON 统一配置多个 MCP 服务，用 MultiServerMCPClient 统一连接和拉取工具，再通过 ToolGateway 把 MCP 工具和本地工具合并，最后统一注入 Agent，让模型自动选择调用。
### 5.SSE 实现LLM回答的分段流式返回，检索 Trace实时输出检索进度 rag_step 与完整溯源信息 Trace
采取SSE的事件，创建一个生产者-消费者队列，每次记录rag_step暂存。
### 6.RAGAS + LangSmith 构建 RAG自动化离线评测与在线监控双闭环
离线闭环：RAGAS 评测
用标准问题集批量跑RAG
检索阶段：召回率、精确率
生成阶段：忠诚度、答案相关性
AB测试
在线闭环：LangSmith监控
用户真实请求自动打trace
记录prompt、检索结果、工具调用、LLM输出、耗时、token使用量
线上发现bad case
回流成离线评测集




项目二：基于 RAG 与 Multi-Agent 电商导购助手
设计基于 LangGraph Manager-Worker 编排的多 Agent 电商导购系统。
细节：
### 1.ReAct + Reflection 机制构建 Multi-Agent 体系

### 2.层次化长上下文 Memory 架构，实现多轮对话上下文保持、历史语义记忆动态召回

### 3.分层式风险治理，Rule Engine + LLM Judge + Human-in-the-Loop 三级合规审查

### 4.内容哈希文档增量更新机制





科研：

人工智能研究方向
----------------

论文：

基于图注意力解决旅行商问题：

主要内容是对单旅行商问题做了强化学习的建模，按顺序一个点一个点选下一个城市”的决策问题，然后用一个图神经网络 + 注意力的模型去学怎么选，再用强化学习把它训好。

研究的是单旅行商问题 TSP：给定若干城市节点，旅行商从起点出发，要求每个城市访问一次且仅访问一次，最后回到起点，使整条路径总长度最短。

先用图神经网络理解城市之间的结构关系，再用注意力解码器一步步选择下一个城市，最后用强化学习优化路径长度。

模型设计：提出了一种基于深度强化学习的多层混合图卷积注意力网络模型，在模型结构上，编码器结合图注意力机制与残差多层图卷积，以增强图结构特征表达能力，解码器采用自适应多头注意力机制，以提升路径构造过程中的节点选择质量，最后，在训练阶段采用 PPO，并进一步引入自适应学习率和动态裁剪阈值机制，提高模型训练的稳定性与收敛效率。实验结果表明，所提方法在单旅行商问题上提升了3个百分点。

编码器：结合图注意力机制与残差多层图卷积 - > 图注意机制：在图结构中为不同邻居节点分配不同的重要性权重，根据节点特征计算邻居对当前节点的重要性分数，在经过softmax归一化得到注意力权重，最后对邻居特征进行加权聚合。计算公式：用一个小的前馈神经网络打分。

强化学习的奖励与惩罚机制：奖励：当前城市i走到下一个城市j，这一步走了多远，就给一个负的距离值，走的越远，奖励越小，走的越近，奖励越大。因为强化学习就是最大化累计回报。

数学建模

研究对象：高速列车轴承智能故障诊断

**源域**：实验台架数据，有标签，适合训练；

**目标域**：真实列车运行数据，基本无标签，更接近实际。

用的迁移学习的方法。

先把振动信号榨成一堆有物理意义的特征，再在实验室数据上把分类器练好，接着用 DANN 把“实验室知识”迁移到真实列车数据上，最后用 SHAP/t-SNE 解释模型为什么这么判。









自我介绍：

你好，我叫葛超，是2026届硕士应届毕业生，研究方向属于人工智能类，在校期间专业排名一直保持在年级前百分之十，多次获得国家励志奖学金和学业奖学金。有多份互联网实习经验，最近的两份实习是马帮科技的ERP3.0 AI驱动的升级迭代业务，我主要负责AI中台接口的编写，完整两阶段的模型推理，准确率达到95%以上。在运去哪的工作是探索RAG系统的检索流程和主导自动化框架的编写。我的职业发展方向是AI应用工程师，并逐步向AI应用架构师方向发展。

职业规划：

​	我本人是特别喜欢做计划的。我对自己做了一个3年预期的规划。有关于技术的学习，也有关于工作上的期望。技术上，我计划用半年时间，快速掌握未来公司开发过程中用到的多种技术，满足在公司日常开发的技术水平，能够熟练的实现产品提出的各种需求。用一年的时间，达到AI中级开发的水平。并且在未来公司里面，成为熟练的技术人员，并得到领导和同事们的认可。

​	然后就是希望在两年内，基于公司现有的项目，不断的去学习新的技术，学习的技术要能够反哺到公司现有的项目中，比如说更好的实现AI应用场景需求等。而且，能够真正具备独立的处理复杂业务的能力。同时我也希望在第三年的时候，我能在公司的工作中，游刃有余。比如说：熟练的处理平时遇到的各种问题，包括技术层面的缺陷以及技术难点。也包括熟练的与其他团队进行高效沟通与协作，快速解决各种层面的问题。

我认为，一个人在公司里面，如果想要得到岗位上的提升，不仅仅是技术上的沉淀，还需要能够熟练的协调不同岗位上的工作流程。所以我的规划是：前两年重点沉淀工程能力和 AI 应用开发能力，第三年希望能够成为团队中比较可靠的核心开发人员，具备独立负责 AI 应用模块和参与架构设计的能力。如果有机会，也希望未来能逐步承担项目组长或 AI 应用架构方向的职责，成为一个让领导放心、让同事信任、能够持续为业务创造价值的技术骨干。





























